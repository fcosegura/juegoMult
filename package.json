{
  "name": "tablasmult",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply tablasmult-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply tablasmult-db --remote",
    "generate:assets": "node scripts/generate-placeholders.mjs",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250517.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.16.0"
  }
}
